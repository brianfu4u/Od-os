# OD 运营系统 · 进度与路线图 (2026-07-09)

## 0 · 一句话现状

**S0→S8 主线全部完成、逐票经 Maestro 独立代码审计、全部合并到 main。** 感知 → 映射 → 交叉验证 → 推理 → 建议 → (批准)执行 → 学习,**六大域**端到端闭环、命令中心接真实数据、会话鉴权、动作回写、学习环全部就位,CI 全绿、可审计、人在环。现进入**上线部署阶段(P5)**。

> 你的角色:看结果、拍板、验收、准备上线所需账号;开发由外部工程师推进,Maestro 逐票独立审代码后再合并。

## 1 · 已完成 · 设计层

结构设计、UI 原型(三语命令中心 + 员工端小程序)、架构可视化、开发计划与排期、逐票实现规格、交接包——全部在仓库 `docs/`(01→20,见 `docs/README` 索引)。

## 2 · 已完成 · 代码层(全部合并到 main)

| PR | 里程碑 | 内容 | 审计 |
|---|---|---|---|
| #1 | S0-1/S0-2 | monorepo + CI + 本体核心 + 多租户 RLS | ✅ |
| #2 | S1-1 | 对象 CRUD + 事件 + SSE 实时 | ✅ |
| #3 | S1-2 | 员工上报接口(小程序)+ 幂等 + 扫码证据 | ✅ |
| #4 | S1-3 | 证据上传 Document/Snapshot(签名下载、去 EXIF) | ✅ |
| #5 | S2 | 交叉验证引擎(护城河) | ✅ |
| #6 | S3 | 域 agent + 指挥编排 → 排序带证据的建议 | ✅ |
| #7 | 命令中心接真数据 | 六域看板 + 运营节拍 + SSE | ✅ |
| #8 | S0-7 base 0.50 | 诚实置信度(冲突 0.50 → 已验证 0.855) | ✅ |
| #9 | 六大域补齐 | 财务/营销/设备 agent + 全域巡检 | ✅ |
| #10 | 演示就绪 | 六域开箱全亮 + 一键 sweep + 验收手册 | ✅ |
| #11 | P1 · S0-3 会话鉴权 | 真登录;生产拒绝自报身份;非超级用户连库 + 启动自检 | ✅ Maestro 逐行审 |
| #12 | P2 · S4 动作回写 | 批准即执行(低风险内部写回)、高风险永不自动执行、append-only 动作日志、幂等、可撤销 | ✅ Maestro 逐行审 |
| #13 | P3 · 命令中心接真数据 + 六域下钻 | 经理登录、真实态势、批准/撤销 UI、下钻+时间线、SSE 重连、三语 | ✅ Maestro 逐行审 |
| #14 | P4 · S8 学习环 | 反馈追加式落库、有界可解释调参、回读入 S2/S3 闭环、可回滚 | ✅ Maestro 逐行审 |

**闭环成立**:感知→映射→交叉验证→推理→建议→执行→学习,六大域全线、可审计、人在环、越用越准。

## 3 · 进行中

- **P5 · 上线部署**(工单 `docs/20`,已下发工程师):Web→Vercel、API→容器托管、DB→托管 Postgres(pgvector),用平台自带 Git 部署(绕开 GitHub Actions 工作流权限限制);生产以非超级用户 `clearview_login` 连库、拒绝自报身份;加最小安全的 staging 经理登录;产出 `docs/DEPLOY.md` 运行手册 + 创始人账号/密钥清单。

## 4 · 接下来

1. **上线部署 P5** → 拿到长期在线、可点的公开 staging 网址。
2. **真实微信小程序客户端** → 需小程序 AppID/AppSecret + 域名 ICP 备案。
3. **真实外部集成**(供应商下单/理赔提交/患者推送/支付) → 需真实凭证 + 合规评估;当前高风险动作只提议、人工执行。
4. **产品化卖多店** → 白标/自助上线、跨店匿名基准(护城河数据资产)、合规。

## 5 · 路线图(阶段 · 里程碑)

| 阶段 | 目标 | 状态 |
|---|---|---|
| Phase 1 · MVP(建议+执行+学习) | 单店闭环 + 六大域 + 命令中心 + S0→S8 | ✅ 完成(逐票审计、全部合并) |
| Phase 2 · 上线可用 | 部署上线 + 真实客户端 + 真实集成 | 🔜 进行中(P5 部署) |
| Phase 3 · 产品化 | 白标 + 自助上线 + 跨店基准 + 合规 | 🧱 地基已具备 |

## 6 · 需要你(创始人)关注/准备

- **上线部署账号**(P5):托管 Postgres(Neon/Supabase,带 pgvector)、API 托管(Render/Railway/Fly)、Vercel;按 `docs/DEPLOY.md` 贴密钥(只贴各平台后台,勿进代码库)。
- **真实微信客户端前置**:企业主体小程序账号 + AppID/AppSecret + API 域名 ICP 备案。
- **合规**:接真实 PHI 前做合规评估(当前一律合成数据)。
- **验收**:可对照《可演示 MVP 验收清单》(`docs/12`)逐项确认闭环。

## 7 · 交付物索引

代码仓:`github.com/brianfu4u/od-os`(私有)。全部规格与路线图在仓库 `docs/`(01→20,见 `docs/README`)。命令中心真数据演示:`docs/demo/index.html`。
