# OD 运营系统 · 进度与路线图 (2026-07-07)

## 0 · 一句话现状

**地基 + 核心闭环 + 命令中心已全部上主干,并可演示。** PR #1–#9 全部合并到 main:**感知 → 映射 → 验证 → 推理 → 建议**,**六大域**端到端跑通、命令中心接真实数据、置信度诚实(0.50)、CI 全绿、可审计、人在环。**命令中心演示可直接打开(真实数据渲染、三语):** https://hyperagent.com/s/8eGrpORC0KYMtBOUVTYWUg 。PR #10(演示就绪)待合并;下一步 **S4(动作回写)**。

> 更新时间 2026-07-08。你的角色:看结果、拍板、验收;开发由工程师推进,机器人处理技术卡点。

## 1 · 已完成 · 设计层

| 交付物 | 说明 |
|---|---|
| 指挥官命令中心 UI 原型(三语) | 店长端 360° 实时看板,会动 |
| 员工端微信小程序原型(中文) | 打卡 / 任务 / 扫码接诊·用设备 / 拍照·语音证据 闭环 |
| 核心结构设计文档 | 本体 · 交叉验证 · agentic loop · 多租户 · 员工终端(含扫码/证据) |
| 架构可视化 | 对象-关系图 · 七阶段循环图 · 分层图 |
| 工程开发计划 + 分工 + 排期 | 技术栈、工作流、2–3 人分工、约 28 周 |
| Sprint 0–1 工单 + S1-1/S1-3 实现规格 | 工程师照做的详细说明 |
| 工程师交接包 | 一页发给工程师即可开工 |

## 2 · 已完成 · 代码层(已合并到 main)

全部 squash 合并到 main(brianfu4u/od-os,最新 SHA 899a85b),CI 全绿。

| PR | 里程碑 | 内容 |
|---|---|---|
| #1 | S0-1 + S0-2 | monorepo + CI/部署骨架;本体核心(状态三元组 + 多租户 RLS 11/11) |
| #2 | S1-1 | 对象增删改查 + 每次改动写事件 + 实时推送 SSE |
| #3 | S1-2 | 员工上报接口(微信小程序模型 POST /reports)+ 幂等 + 扫码作证据 |
| #4 | S1-3 | 证据上传 → Document/Snapshot(签名下载、去 EXIF、按 kind 限制) |
| #5 | S2 | 交叉验证引擎(冲突 → 补证据 → 已验证,不可变账本) |
| #6 | S3 | 域 agent + 指挥编排器 → 排序、带证据的店长建议(人在环) |
| #7 | 命令中心接真数据 | 六域看板 + 运营节拍 + SSE + CI(含全应用 HTTP 冒烟) |
| #8 | S0-7 base 0.50 | 诚实置信度(裸声称=五五开;冲突 0.50 → 已验证 0.855) |
| #9 | 六大域补齐 | 财务/营销/设备 三个 agent + 六域瓦片接真数据 + 全域巡检 sweep |

验证:build 3/3 · 单测 58 · 集成 107(RLS 11)· 全应用 HTTP 冒烟 30/30(六域全覆盖)。

## 3 · 进行中

- **PR #10「演示就绪」**(待合并,CI 跑绿即合):补了员工域种子建议(**六个域开箱即全亮**)、员工控制台加**一键 Run sweep 按钮**、`docs/05` 三条命令本地运行 + 验收手册。验证:build 3/3 · 单测 58 · 集成(sweep 15)· HTTP 冒烟 30/30。
- **演示可看(真实数据渲染 · 三语 · 默认中文)**:https://hyperagent.com/s/8eGrpORC0KYMtBOUVTYWUg —— 六域瓦片、AI 副驾建议流、冲突 0.50 → 已验证 0.855 账本都在里面。
- **验收**:可对照《可演示 MVP 验收清单》逐项确认(报告→冲突→照片→翻绿;六域各出建议;批准只记录;租户隔离;三语)。

## 4 · 接下来(近期)

1. **合并 PR #10**(CI 绿后)——让演示开箱即用。
2. **验收演示**——你照清单亲自确认闭环(看链接即可,或让工程师给本地/在线入口)。
3. **S4(动作回写)**——让**已批准的低风险、可逆动作**自动执行(改派、补货、停用设备并改路由等);高风险仍人工;全程人在环、走审批闸门。规格待写。
4. 之后:**真微信小程序客户端**(独立前端工作流,需小程序账号 + ICP 备案,沙箱内建不了)、**长期在线 staging 网址**(连 Vercel + 托管数据库)、学习环、六域下钻页。

## 5 · 路线图(阶段 · 里程碑)

| 阶段 | 目标 | 状态 |
|---|---|---|
| Phase 1 · MVP(只出建议) | 单店闭环 + 六大域全活 + 命令中心可演示 | ✅ **功能完整、可演示**(待你验收 + 合 PR #10) |
| Phase 2 · 全功能 | S4 动作回写(审批闸门)+ 学习环 + 六域下钻 | 🔜 **下一步(S4)** |
| Phase 3 · 产品化 | 多租户上线、真微信小程序、长期 staging、白标、跨店基准 | 🧱 已有多租户地基;上线前办小程序账号 + ICP + 连 Vercel/DB |

> 一句话:**Phase 1(建议模式)基本拿下,准备迈入 Phase 2 的第一步 S4。**

## 6 · 需要你(创始人)关注/准备的事

- **合并 PR #3**(若工程师连接合并不了,你点一下 Squash and merge)。
- **CI(可选)**:想要自动检查就把那 1 行 bug 修了(变绿);不想要就忽略/删掉。
- **推代码权限(已知限制)**:当前 GitHub 连接是 Copilot 型,只能逐个推(合并时 squash 即干净),且改 CI/工作流文件需你在网站上动手。想一劳永逸可换 PAT 型连接(以后再说)。
- **上线前(Phase 3)要办**:注册微信小程序账号(企业主体)、后端 API 域名 ICP 备案、连 Vercel + 数据库(Supabase/腾讯云 COS)、微信登录并入认证。
- **可提供的信息**:诊所的 5 个关键任务类型 + 各自 SOP 与「必需证据」(用于 S0-7 与交叉验证)。

## 7 · 交付物索引(链接)

**可直接打开的原型/文档(公开链接):**
- 指挥官命令中心 UI(三语):https://hyperagent.com/s/Mp6B_aWzyOrH4OAyHERxrA
- 架构可视化:https://hyperagent.com/s/XCitBYifouAb81oRt8h4yA
- 核心结构设计(.md):https://pub.hyperagent.com/api/published/pbf01KWX93C03_9CQGR296QM1GCKD8/01-structure-design.md
- 工程开发计划(.md):https://pub.hyperagent.com/api/published/pbf01KWX93CGT_7CSVDWW6SGPE31MS/02-dev-plan.md
- Sprint 0–1 工单(.md):https://pub.hyperagent.com/api/published/pbf01KWX93CTJ_DHGDJ5DQ28JYAMAT/03-sprint-0-1-tickets.md

**在你的 Hyperagent 库里(可再导出为链接):** 员工端微信小程序原型、结构设计文档(已更新含员工终端)、开发计划(已更新)、S1-1/S1-3 实现规格、工程师交接包。

**代码仓:** github.com/brianfu4u/od-os(私有)。
